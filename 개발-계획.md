# 폴드노트 개발 계획

갤럭시 Z 폴드8에 최적화된 노트 + 다이어리 앱 **폴드노트(FoldNote)** 개발 계획.

## 목표

- 폴드8의 두 화면에 모두 자연스러운 노트 앱
  - **커버 화면** (5.5인치, 1972×1248, 세로형 ≈ CSS 416×657px): 한 장씩 보는 단일 화면 + 하단 탭
  - **메인 화면** (7.6인치, 2448×1848, 4:3 ≈ CSS 816×616px): 수첩 펼침면처럼 좌(목록/달력)·우(편집) 두 쪽 화면
- 다이어리 기능 필수: 달력 기반 하루 기록 + 기분/날씨 이모지
- 오프라인에서도 동작, 데이터는 기기(브라우저)에 저장

## 기술 선택

**웹 앱(PWA)** 으로 구현한다.

- 이 환경에서는 안드로이드 네이티브 빌드·에뮬레이터 검증이 불가능하고, PWA는 폴드8 브라우저에서 바로 열어 홈 화면에 설치해 쓸 수 있다.
- 빌드 도구 없이 정적 파일(`app/index.html`)만으로 동작 → 유지보수와 배포(GitHub Pages 등)가 단순하다.
- 추후 네이티브(Kotlin + Jetpack Compose) 포팅 시에도 이번 버전이 UX 프로토타입 역할을 한다.

## 기능 명세 (v1)

### 노트
- 노트 생성/편집/삭제, 제목 + 본문
- 자동 저장(입력 즉시), 수정 시각·글자 수 표시
- 검색(제목+본문), 고정(핀) 기능
- 대화면: 목록(왼쪽) + 편집기(오른쪽) 동시 표시

### 다이어리
- 월 달력 뷰, 날짜 선택 → 그날의 기록 작성 (하루 1개)
- 기분 이모지 6종, 날씨 6종 선택
- 기록 있는 날은 달력에 기분 이모지 표시
- 이전/다음 달 이동, "오늘" 바로가기

### 공통
- 라이트/다크/시스템 테마
- 데이터 JSON 내보내기/가져오기, 전체 삭제
- PWA: 매니페스트 + 서비스 워커(오프라인 캐시), 홈 화면 설치
- `#demo` 해시로 열면 샘플 데이터 자동 생성(체험용)

## 폴드8 대응 설계

| 항목 | 커버 화면 | 메인 화면(펼침) |
|------|----------|----------------|
| 기준 미디어쿼리 | `max-width: 699px` | `min-width: 700px` |
| 내비게이션 | 하단 탭 바 | 상단 바(로고 + 탭) |
| 노트 | 목록 ↔ 편집 화면 전환(스택) | 목록 + 편집 두 쪽 펼침면 |
| 다이어리 | 달력 ↔ 기록 화면 전환 | 달력 + 기록 두 쪽 펼침면 |
| 힌지 표현 | - | 화면 정중앙 50:50 분할 — 접힘선이 실제 힌지 위치와 겹쳐 수첩처럼 보임 |

- 터치 타깃 44px 이상, `prefers-reduced-motion` 존중
- 다크 모드: 시스템 설정 + 앱 내 수동 전환 모두 지원

## 데이터 모델

```
localStorage["foldnote.v1"] = {
  notes: [{ id, title, body, pinned, createdAt, updatedAt }],
  diary: { "YYYY-MM-DD": { date, mood, weather, body, updatedAt } },
  settings: { theme: "system" | "light" | "dark" }
}
```

## 검증 계획

- 커버 화면(412×660)·메인 화면(816×616) 뷰포트로 헤드리스 크로미움 스크린샷 확인
- 스크립트 문법 검사(node --check)

## 로드맵

유사 앱 조사에 기반한 상세 로드맵은 [벤치마킹-개선점.md](벤치마킹-개선점.md) 참고.

- **v1 (완료)**: 위 기능 전체 + 정중앙 50:50 펼침면
- **v1.1 (완료)**: 다이어리 통계·스트릭, 활동 태그, 체크리스트, 휴지통, PIN 잠금, 지난 오늘
- **v2 (완료)**: IndexedDB 이전(localStorage 자동 마이그레이션), 노트·다이어리 사진 첨부(자동 리사이즈), 플렉스 모드(`device-posture` — 반 접으면 위=보기/아래=쓰기), 달력 페이지 넘김 모션, PWA 바로가기(새 노트/오늘 일기)
  - 보류: 알림 리마인더(웹 푸시 서버 필요), 리치 텍스트 서식(에디터 구조 개편 필요) → v3 후보
- **v3**: 클라우드 동기화(E2E 암호화), 다중 저널·템플릿, AI 회고, 알림 리마인더, 서식, 네이티브(Compose) 포팅 검토
