# 단어카드 그림 — AI 생성 프롬프트 묶음 (말랑 점토 스타일)

아기 단어카드(`cards/`)에 넣을 그림 **56장**을 AI로 만들기 위한 프롬프트다.
색깔(8)·숫자(10)·모양(5) **23장은 생성하지 않는다** — 빨간 동그라미나 숫자 3은
코드로 그린 도형이 곧 정답이고, 생성하면 오히려 흐려진다.

**스타일은 말랑말랑한 점토(클레이) 3D 하나로 통일한다.** 둥글고 통통해서 귀여운데
입체감 덕에 외곽이 또렷하다 — 12~36개월이 그림을 알아보는 데 제일 중요한 조건이다.
동물부터 숟가락, 아이 얼굴, 해·무지개까지 같은 결로 나오므로 접두문 하나로 56장을 덮는다.

## 자동 생성 — OpenAI API (기본 경로)

사람이 한 장씩 뽑지 않는다. `tools/cards-generate.mjs` 가 **이 문서의 접두문과 표를 그대로
읽어** OpenAI 이미지 API로 만들고, 다듬어서(`cards-photo-prep.py`) 앱에 넣는다(`cards-photos.mjs`).
프롬프트를 고치려면 이 문서를 고친다 — 스크립트에 따로 적어 두지 않았다.

```sh
# 준비: 클라우드 환경 설정 → 환경 변수에 OPENAI_API_KEY (채팅에 붙여넣지 않는다), 새 세션에서
pip install Pillow
node tools/cards-generate.mjs --dry-run                      # 보낼 프롬프트 확인 (요청 안 함)
node tools/cards-generate.mjs                                # 시범 4장 — 강아지·사과·자동차·컵
node tools/cards-check.mjs --shots                           # 카드에 얹힌 모습 확인
node tools/cards-generate.mjs --only fruits-banana --force   # 마음에 안 드는 장만 다시
```

승인 뒤 나머지는 **몸만 따로** 돌린다. 몸 프롬프트는 "the same toddler(같은 아이)"로 이어지는데
API 호출은 서로 독립이라, 첫 장을 참조로 물리지 않으면 여덟 장이 다 다른 아이가 된다.

```sh
R=cards/img/animals-dog.webp
for c in animals fruits vegetables vehicles things nature; do
  node tools/cards-generate.mjs --ref $R --category $c        # 강아지에 결 맞춤
done
node tools/cards-generate.mjs --ref $R --only body-eyes       # 몸 첫 장 — 이 아이가 기준이 된다
node tools/cards-generate.mjs --ref cards/img/body-eyes.webp --category body   # 나머지 일곱 장
```

- **투명 배경을 API가 직접 만든다**(`background: transparent`). 그래서 접두문의 "plain pure white
  background" 를 스크립트가 "transparent background" 로 바꿔 보낸다 — 문서의 그 문구를 바꾸면
  스크립트가 멈추고 알려준다 (조용히 흰 판이 그려지는 것보다 낫다)
- 원본 PNG는 `.gen/`(저장소 제외), 다듬은 WebP는 `cards/img/`(커밋). 이미 받은 원본은 건너뛰므로
  중간에 끊겨도 다시 돌리면 이어서 한다
- 429·5xx는 기다렸다 다시(최대 3번). **403 "organization must be verified"** 가 나면 OpenAI 쪽
  조직 인증이 필요하다는 뜻이다 — platform.openai.com → Settings → Organization → Verify
- 모델은 `OPENAI_IMAGE_MODEL`(기본 `gpt-image-1`), 품질은 `--quality`(기본 high)
- 흐름 전체를 가짜 API 서버(`OPENAI_BASE_URL`)로 시험했다: 생성·참조 편집·429 재시도·403 안내·
  이어하기·투명 판별·가운데 정렬·목록 갱신

아래 "먼저 4장만" 이하는 사람이 직접 뽑을 때의 안내다. 자동 생성에서도 **4장을 먼저 보고
승인받은 뒤 나머지를 돌린다**는 순서는 같다.

## 먼저 4장만

**강아지 · 사과 · 자동차 · 컵.** 이 네 장을 뽑아 보내주면 카드에 얹어서 보여드린 뒤
나머지를 진행한다. 스타일이 갈리면 56장을 다시 뽑아야 하므로 이 4장이 제일 중요하다.

## 스타일 접두문

각 항목 프롬프트 **앞에 그대로 붙여 쓴다.** 이 문장이 56장의 통일감을 만든다.

```
Cute 3D claymation style illustration for a toddler's flashcard. A single subject,
centered, entire subject visible with a small margin around it, plain pure white
background, smooth rounded plasticine shapes with a soft matte clay texture and
gentle fingerprint-like surface, chubby friendly proportions, bright cheerful
pastel colors, soft even studio lighting, very soft shadow, simple and clean,
nothing scary, square 1:1, no text, no letters, no watermark, no logo.
Subject:
```

**같은 결을 유지하는 요령** — 두 번째 장부터는 첫 장을 참조 이미지로 같이 넣고
"같은 점토 스타일, 같은 조명, 같은 흰 배경으로" 라고 덧붙이면 세트가 흐트러지지 않는다.
한 대화에서 이어서 뽑는 것도 효과가 있다.

## 보내는 방법

- 형식은 **PNG·JPG 아무거나**, 크기도 상관없다 (클수록 좋다)
- **파일명은 바꾸지 않아도 된다.** 카테고리별로 아래 표 순서대로 보내주면 이름을 맞춘다
- **배경은 흰색으로.** 받아서 흰 배경을 투명하게 빼드린다 — 그래야 어두운 테마에서
  흰 사각형이 뜨지 않는다 (`tools/cards-photo-prep.py` 가 한다)
- 마음에 안 드는 장이 있으면 그 단어만 다시 보내면 된다

---

## 동물 8장

| 파일명 | 프롬프트 (접두문 뒤에 붙인다) |
|---|---|
| `animals-dog.webp` | a happy puppy sitting, facing forward, floppy ears, tongue out |
| `animals-cat.webp` | a kitten sitting, facing forward, pointy ears, long whiskers |
| `animals-rabbit.webp` | a white rabbit sitting, both long ears standing up, facing forward |
| `animals-bear.webp` | a brown bear cub sitting, round ears, facing forward |
| `animals-lion.webp` | a lion with a big round golden mane, sitting, gentle smiling face |
| `animals-elephant.webp` | a baby elephant standing, big ears, trunk curled down, facing forward |
| `animals-pig.webp` | a pink piglet standing, round snout, facing forward |
| `animals-chick.webp` | a fluffy yellow baby chick standing, tiny orange beak, facing forward |

## 과일 8장

| 파일명 | 프롬프트 |
|---|---|
| `fruits-apple.webp` | one red apple with a short brown stem and one green leaf |
| `fruits-banana.webp` | a bunch of three yellow bananas |
| `fruits-strawberry.webp` | one red strawberry with a green leafy crown and tiny seeds |
| `fruits-grapes.webp` | a bunch of purple grapes with one green leaf |
| `fruits-watermelon.webp` | one triangular slice of watermelon, red flesh, green rind, black seeds |
| `fruits-tangerine.webp` | one orange tangerine with one green leaf on top |
| `fruits-peach.webp` | one pink peach with one green leaf |
| `fruits-cherry.webp` | two red cherries joined at one stem with a green leaf |

## 채소 8장

| 파일명 | 프롬프트 |
|---|---|
| `vegetables-carrot.webp` | one orange carrot with a green leafy top |
| `vegetables-tomato.webp` | one round red tomato with a small green calyx |
| `vegetables-cucumber.webp` | one green cucumber, whole |
| `vegetables-corn.webp` | one ear of yellow corn with green husk leaves peeled back |
| `vegetables-eggplant.webp` | one purple eggplant with a green cap |
| `vegetables-broccoli.webp` | one head of green broccoli with a short pale stalk |
| `vegetables-potato.webp` | one brown potato, whole |
| `vegetables-pumpkin.webp` | one round orange pumpkin with a short brown stem |

## 탈것 8장

점토라 상표가 붙을 일은 거의 없지만, 차체에 글자가 박히면 아기 카드로는 못 쓴다.
프롬프트 끝의 **no text on the body** 를 지우지 말 것.

| 파일명 | 프롬프트 |
|---|---|
| `vehicles-car.webp` | a small round red car, three-quarter front view, no text on the body |
| `vehicles-bus.webp` | a yellow school bus, three-quarter front view, no text on the body |
| `vehicles-train.webp` | a toy steam locomotive with one carriage, side view, no text on the body |
| `vehicles-airplane.webp` | a white and blue passenger airplane, three-quarter front view, no text on the body |
| `vehicles-ship.webp` | a small wooden sailboat with one red and white sail |
| `vehicles-bicycle.webp` | a blue bicycle, side view, no text on the body |
| `vehicles-fire-truck.webp` | a red fire truck with a ladder on top, three-quarter front view, no text on the body |
| `vehicles-helicopter.webp` | a blue helicopter with a main rotor and tail rotor, three-quarter front view |

## 생활용품 8장

| 파일명 | 프롬프트 |
|---|---|
| `things-cup.webp` | one purple mug with a round handle, seen from the side |
| `things-spoon.webp` | one spoon lying flat, bowl at the top, handle pointing down |
| `things-toothbrush.webp` | one blue toothbrush with white bristles and a small dab of mint toothpaste |
| `things-shoes.webp` | a pair of small red children's sneakers side by side |
| `things-hat.webp` | one blue baseball cap, three-quarter view |
| `things-bag.webp` | one purple children's backpack standing upright |
| `things-clock.webp` | one round alarm clock with two bells on top, hands at three o'clock |
| `things-umbrella.webp` | one open umbrella with red and yellow panels and a wooden handle |

## 몸 8장

같은 아이를 여덟 번 쓰는 셈이라 **한 대화에서 이어서 뽑는 게 좋다.**
첫 장을 뽑고 "같은 아이로, 이번에는 코를 강조해서" 하는 식으로 이어가면 얼굴이 유지된다.

| 파일명 | 프롬프트 |
|---|---|
| `body-eyes.webp` | a cheerful toddler's head, big round eyes wide open and clearly emphasized |
| `body-nose.webp` | the same toddler's head, the small round nose clearly emphasized |
| `body-mouth.webp` | the same toddler's head, mouth open in a wide happy smile |
| `body-ears.webp` | the same toddler's head from the front, both ears clearly emphasized |
| `body-teeth.webp` | a smiling mouth showing a row of clean white teeth, close up |
| `body-hand.webp` | a child's open hand, palm facing forward, five chubby fingers spread |
| `body-foot.webp` | a child's bare foot from the side, five round toes |
| `body-hair.webp` | the same toddler's head with soft brown hair clearly emphasized |

## 자연 8장

여기도 흰 배경으로 통일한다 (하늘을 넣지 않는다 — 배경을 투명하게 빼야 한다).

| 파일명 | 프롬프트 |
|---|---|
| `nature-sun.webp` | a yellow sun with chunky triangular rays and a gentle smiling face |
| `nature-moon.webp` | a pale yellow crescent moon with a gentle sleeping face |
| `nature-star.webp` | one yellow five-pointed star with a gentle smiling face |
| `nature-cloud.webp` | one fluffy white cloud with soft round bumps |
| `nature-rain.webp` | a gray cloud with three blue raindrops falling below it |
| `nature-rainbow.webp` | a rainbow arch of seven colors with a small white cloud at each end |
| `nature-tree.webp` | one tree with a round green leafy top and a short brown trunk |
| `nature-flower.webp` | one pink flower with rounded petals, a yellow center, a green stem and two leaves |

---

## 받은 뒤 내가 하는 일

```sh
pip install Pillow                                              # 처음 한 번
python3 tools/cards-photo-prep.py --category animals <파일들...>   # 순서대로 이름 배정
node tools/cards-photos.mjs                                     # 앱이 보는 사진 목록 갱신
node tools/cards-check.mjs --shots                              # 점검 + 스크린샷
```

`cards-photo-prep.py` 가 하는 일

1. 가장자리에서 이어지는 **흰 배경을 투명하게** 뺀다 (흰 토끼처럼 안쪽 흰색은 그대로 둔다)
2. 여백을 잘라내고 **정사각형 가운데**에 6% 여백을 두고 앉힌다 — 카드마다 크기가 들쭉날쭉하지 않게
3. **640×640 WebP** 로 저장 (장당 40~80KB, 56장 다 해도 3MB 안팎)

`--names <카테고리>` 로 그 카테고리에 필요한 파일명을 순서대로 볼 수 있다.

## 알아 둘 것

- **그림 파일이 없는 카드는 지금의 SVG가 그대로 보인다.** 56장을 한꺼번에 채울 필요가 없고,
  카테고리 하나씩 바꿔 나가도 앱은 늘 정상으로 보인다
- 생성물의 사용권은 만든 쪽(계정 소유자)에게 있고 플레이도 AI 생성물을 허용한다.
  다만 **상표·실존 인물·기존 캐릭터가 섞이지 않게** 프롬프트에 no text, no logo 를 넣어 뒀다
- 글자가 박혀 나온 장은 버린다. 아기 카드에 영문 워터마크가 남으면 심사에서도 걸린다
- 배경이 흰색이 아니면 투명화를 건너뛴다 (그래도 동작은 한다). 되도록 흰 배경으로 받는다
