# 아기 단어카드 (`cards/`)

아기와 함께 보는 단어카드 웹 앱. 휴대폰·태블릿 모두 대응. 만든 곳 **Yoonque Labs**.
무료·광고 없음, 권한 0개, 인터넷 없이 동작.

- 주소(머지 후): **https://ysh0525.github.io/note/cards/**
- 파일: `cards/index.html` 한 장 + `manifest.webmanifest` · `sw.js` · `icon.svg` · `img/`(카드 사진)
- 폴드노트(`app/`)와 완전히 분리돼 있다. 같은 저장소에 있을 뿐 코드·저장소(localStorage 키)가 겹치지 않는다

## 무엇이 들어 있나

카테고리 10개 · 카드 **79장**.

| 카테고리 | 장수 | 단어 |
|---|---|---|
| 동물 | 8 | 강아지 고양이 토끼 곰 사자 코끼리 돼지 병아리 |
| 과일 | 8 | 사과 바나나 딸기 포도 수박 귤 복숭아 체리 |
| 채소 | 8 | 당근 토마토 오이 옥수수 가지 브로콜리 감자 호박 |
| 탈것 | 8 | 자동차 버스 기차 비행기 배 자전거 소방차 헬리콥터 |
| 생활용품 | 8 | 컵 숟가락 칫솔 신발 모자 가방 시계 우산 |
| 몸 | 8 | 눈 코 입 귀 이빨 손 발 머리카락 |
| 자연 | 8 | 해 달 별 구름 비 무지개 나무 꽃 |
| 색깔 | 8 | 빨강 주황 노랑 초록 파랑 보라 분홍 하양 |
| 숫자 | 10 | 하나 ~ 열 (숫자꼴 + 그 개수만큼의 동그라미) |
| 모양 | 5 | 동그라미 세모 네모 하트 마름모 |

## 화면과 조작

- **표지** — 카테고리 타일. 타일 그림은 첫 카드를 쓰되, 색깔·모양처럼 첫 카드가 겹쳐 보이는
  카테고리는 `icon` 을 따로 둔다
- **카드** — 그림을 누르면 이름을 읽어준다. 좌우로 밀거나 아래 큰 화살표로 넘긴다
  (키보드 ←→ 도 된다)
- **섞기** — 순서를 섞고 바로 다음 장부터 보여준다
- **자동 넘김** — 3·5·8초 간격. 손으로 넘기면 시계를 다시 센다
- **아기 잠금** — 자물쇠를 누르면 뒤로·섞기·자동 버튼이 숨어 아기가 빠져나가지 못한다.
  풀 때는 자물쇠를 **2초 길게**. (짧게 누르면 안 풀린다 — 아기 손가락 대비)
- **설정** — 소리 끄기, 말하기 속도 3단, 영어 함께 보기, 자동 넘김 간격, 테마(기기/밝게/어둡게).
  `localStorage["wordcards.settings"]` 에 저장한다

`#animals`, `#numbers/9` 처럼 주소로 바로 들어갈 수 있고, 폰 뒤로가기로 표지에 돌아온다.

## 그림 — 사진과 SVG를 겹쳐 쓴다

카드 그림은 두 겹이다. **바탕은 코드로 그린 인라인 SVG**, 그 위에 같은 이름의 사진
파일이 있으면 사진이 덮는다. 그래서 56장을 한꺼번에 채울 필요가 없고, 한 장씩
바꿔 나가도 앱은 늘 정상으로 보인다.

- 파일 위치·이름: `cards/img/<카테고리>-<영어단어>.webp` (예 `animals-dog.webp`,
  `vehicles-fire-truck.webp`). `imgFile()` 이 카드 데이터에서 그대로 만들어 낸다
- **사진을 넣거나 뺀 뒤에는 반드시 `node tools/cards-photos.mjs`** — `index.html` 의
  `PHOTOS` 목록을 다시 쓴다. 앱은 이 목록에 있는 파일만 불러오므로(없는 파일을 불러
  404를 내지 않으려고) **목록을 안 고치면 사진이 조용히 안 보인다.**
  `tools/cards-check.mjs` 가 폴더와 목록이 어긋나는지 본다
- 색깔·숫자·모양은 사진을 쓰지 않는다 (`photos: true` 를 주지 않았다). 빨간 동그라미나
  숫자 3은 도형이 곧 정답이다
- 사진은 첫 실행 뒤 한가할 때 한 장씩 캐시에 받아 둔다 — 비행기 모드에서도 다 나오게
- 그림체는 **말랑 점토(클레이) 3D 하나로 통일**한다. 둥글어서 귀여운데 입체감 덕에 외곽이
  또렷해 아기가 알아보기 쉽다. 생성 프롬프트: [단어카드-그림-프롬프트.md](단어카드-그림-프롬프트.md)
- 받은 그림은 `python3 tools/cards-photo-prep.py --category <카테고리> <파일들>` 로 다듬는다.
  **흰 배경을 투명하게 빼고**(어두운 테마에서 흰 사각형이 뜨지 않게, 안쪽 흰색은 남긴다)
  여백을 정리해 640×640 WebP 로 저장한다

> **CC0 사진 검색은 접었다.** 동물 8종으로 시험해 보니 강아지 자리에 강아지 모양 벽시계와
> 상표가 찍힌 장난감 상자(CC0로 잘못 표기된 것), 고양이 자리에 사람 얼굴(초상권은 저작권과
> 별개다), 사자는 생고기를 뜯는 장면이 나왔다. 저작권보다 품질·안전·상표 오표기가 문제였다.

## 만든 방식에서 기억할 것

- **바탕 그림은 전부 코드로 그린 인라인 SVG다.** 이모지를 쓰지 않은 이유는 기기·OS 버전마다
  모양이 달라지고, 생활용품 쪽은 마땅한 이모지가 없는 단어가 많아서다. viewBox 는
  `0 0 120 120` 으로 통일한다
- **그림은 밝은 바탕을 전제로 그렸다.** 그래서 어두운 테마에서도 카드의 그림칸에는
  밝은 판(`--art-bg`)을 깔고, 글씨만 어두운 카드 위에 올린다
- **카드 화면 배경은 카테고리 색으로 물들인다.** 밝을 때는 `soft`, 어두울 때는 `deep`.
  한 쪽만 두면 어두운 테마에서 제목 글씨가 파스텔 배경에 묻힌다
- **그림칸은 절대배치로 채운다.** 부모 높이가 flex 로 정해져도 자식의 `height:100%` 가
  auto 로 풀려 그림이 작아지는 경우가 있다
- **사진은 `pointer-events: none` 이어야 한다.** 안 그러면 사진 위에서 끌 때 브라우저의 이미지
  끌어다 놓기가 스와이프를 취소시키고, 안드로이드에선 길게 누를 때 '이미지 저장' 메뉴가 뜬다.
  실제 사진을 넣고서야 드러난 버그라, 사진이 없어도 잡히는 점검을 따로 붙였다
- **잠금 해제 직후의 click 을 막아야 한다.** `pointerdown` 으로 길게 눌러 풀면 손을 뗄 때
  click 이 이어서 날아와 그대로 다시 잠긴다 (`holdDone` 플래그로 한 번 삼킨다)

## 검증

```sh
node tools/cards-generate.mjs       # 그림 생성 (OPENAI_API_KEY 필요, --dry-run 은 불필요)
node tools/cards-photos.mjs         # 사진을 넣거나 뺀 뒤 — PHOTOS 목록 다시 쓰기
node tools/cards-check.mjs          # 문법 + 동작 25가지 점검
node tools/cards-check.mjs --shots  # 스크린샷까지 (.shots/cards/, 저장소에는 안 들어간다)
```

점검 항목: 스크립트 문법, 카드 데이터(한글·영어·그림 누락, 단어 중복), 넘기기(버튼·스와이프·
순환), 섞기, 자동 넘김 시작/정지, 아기 잠금(짧게는 안 풀림·2초는 풀림), 설정 저장,
주소 진입과 뒤로가기, 사진 목록과 폴더 일치, 사진이 터치를 가로채지 않음, 페이지 오류 0건.

## 안드로이드로 포장한다면

아직 웹 앱만 있다. 폴드노트처럼 웹뷰로 감쌀 때 미리 알아둘 것:

- **소리가 제일 먼저 막힐 자리다.** 안드로이드 웹뷰는 Web Speech API 음성 합성을
  지원하지 않는 것으로 알려져 있다(크롬에서는 된다). 포장하면 **실기기에서 소리부터
  확인하고**, 안 나면 `android.speech.tts.TextToSpeech` 를 자바 쪽에 두고
  `@JavascriptInterface` 로 잇는다 (`Bridge.java` 방식). 앱 코드의 `speak()` 한 곳만
  갈아 끼우면 되도록 소리 호출은 그 함수 하나로 모아 뒀다
- **엣지 투 엣지** — targetSdk 35+ 는 상태바가 웹뷰 위에 겹쳐 그려진다. CSS
  `env(safe-area-inset-top)` 은 안드로이드에서 상태바 높이를 0으로 주므로 소용없다.
  폴드노트의 `MainActivity` 처럼 실측값을 CSS 변수로 써넣어야 한다 (`CLAUDE.md` 참고)
- 권한은 **0개를 유지한다.** 진동(`VIBRATE`)도 권한이 필요해서 쓰지 않았다
- 패키지 이름·키스토어는 새로 정해야 한다. 순서는
  [store/플레이-출시-플레이북.md](store/플레이-출시-플레이북.md) 를 그대로 따른다

## 지금 어디까지 왔나 — 다음 세션 인수인계

**그림을 OpenAI API로 생성하기로 했다** (2026-09-23). 사진(CC0)은 품질·상표 문제로, 코드로 그린
점토풍 SVG는 "원하는 느낌이 아니다"로 접었다. 그림체는 **말랑 점토 3D**로 확정.

남은 일, 순서대로:

1. 사용자가 클라우드 환경 변수에 `OPENAI_API_KEY` 를 넣는다 → **새 세션**에서 이어간다
2. PR #28 이 아직 안 머지됐으면 그 브랜치에서 한다:
   `git fetch origin claude/tender-gates-xogl7a && git checkout claude/tender-gates-xogl7a`
3. `pip install Pillow` → `node tools/cards-generate.mjs` (시범 4장) → `node tools/cards-check.mjs --shots`
4. **4장을 스크린샷으로 사용자에게 보이고 승인을 받는다.** 스타일이 갈리면 접두문을 고쳐 4장만 다시
5. 승인되면 나머지 — 절차는 [단어카드-그림-프롬프트.md](단어카드-그림-프롬프트.md) "자동 생성".
   **몸은 따로** 돌린다: `body-eyes` 한 장을 먼저 만들고 그걸 `--ref` 로 물려 나머지 일곱 장
   (프롬프트가 "같은 아이"로 이어지는데 API 호출끼리는 기억이 없다)
   → 전장을 한 장에 모은 검수 시트로 확인 → 글자·상표·무서운 표정이 섞인 장은 `--only ... --force`
6. 커밋(`cards/img/*.webp` + `index.html` 의 PHOTOS) → 푸시 → 사용자에게 보고

그림이 들어오면 **코드로 그린 SVG는 사진이 없을 때의 대체물**로만 남는다. 지우지 않는다.

## 다음 후보

- 낱말 맞히기(그림 세 장 중 고르기) — 24개월 이후
- 부모가 직접 카드 추가 (사진 없이 그림 고르기 + 이름 적기)
- 카테고리별 진도·오늘 본 카드 수
- 녹음한 부모 목소리로 읽어주기 (기기 TTS 대신)
